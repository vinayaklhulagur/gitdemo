git init
git add index.htm
git rm --cached index.htm
git config --global user.email <useremail>
git config --global user.name <username>
git status
git add *
git add -A
git rm --cached gitcommands.txt -f
git commit -m "initial version created"
git log
